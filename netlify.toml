# Netlify: збірка й публікація (Cloudflare Pages бере ті самі
# налаштування з UI: build command "npm run build", output "dist")
[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
