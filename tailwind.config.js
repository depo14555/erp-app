/**
 * Стиль системи — «інженерний»: щільно, точно, без пухких форм.
 *
 * Усе задано ТУТ, а не по компонентах: у коді лишаються ті самі
 * rounded-2xl і shadow-lg, але означають вони тепер інше. Тому
 * зміна стилю не потребує правок у сотні місць.
 *
 * @type {import('tailwindcss').Config}
 */
export default {
  content: ['./index.html', './src/**/*.{js,ts,jsx,tsx}'],
  theme: {
    extend: {
      /**
       * Радіуси менші за типові «застосункові». Прилад не буває
       * заокругленим — око читає прямі кути як точність.
       */
      borderRadius: {
        none: '0',
        sm: '4px',
        DEFAULT: '6px',
        md: '7px',
        lg: '8px',
        xl: '10px',
        '2xl': '12px',
        '3xl': '14px',
        full: '9999px',
      },

      /**
       * Тіні лише там, де щось справді лежить НАД сторінкою.
       * Замість розмитої хмари — щільний контур плюс коротка тінь.
       */
      boxShadow: {
        sm: '0 1px 2px rgba(16, 24, 40, 0.05)',
        DEFAULT: '0 1px 2px rgba(16, 24, 40, 0.06), 0 1px 3px rgba(16, 24, 40, 0.10)',
        md: '0 2px 4px -2px rgba(16, 24, 40, 0.06), 0 4px 8px -2px rgba(16, 24, 40, 0.10)',
        lg: '0 4px 6px -2px rgba(16, 24, 40, 0.03), 0 12px 16px -4px rgba(16, 24, 40, 0.08)',
        xl: '0 8px 8px -4px rgba(16, 24, 40, 0.03), 0 20px 24px -4px rgba(16, 24, 40, 0.08)',
        '2xl': '0 24px 48px -12px rgba(16, 24, 40, 0.18)',
        none: 'none',
      },

      /**
       * Сірий — холодний і рівний по кроках. Це «корпус приладу»,
       * на якому колір з'являється лише там, де щось означає.
       */
      colors: {
        /*
          Світлий CRM: білий — справді білий аркуш, сірий — холодний і
          рівний по кроках. Підміна тут перефарбовує одразу все, що
          написано bg-white / bg-gray-50 у сотні місць.
        */
        white: '#FFFFFF',
        gray: {
          50: '#F7F8FA',
          100: '#F1F3F6',
          200: '#E7E9EE',
          300: '#D6DAE2',
          400: '#B4BBC6',
          500: '#98A1AE',
          600: '#5B6472',
          700: '#3D4552',
          800: '#242A33',
          900: '#101418',
        },
      },

      fontFamily: {
        sans: ['Inter', 'system-ui', '-apple-system', 'Segoe UI', 'Roboto', 'sans-serif'],
        mono: ['IBM Plex Mono', 'ui-monospace', 'SFMono-Regular', 'Consolas',
               'Liberation Mono', 'monospace'],
      },
    },
  },
  plugins: [],
};
